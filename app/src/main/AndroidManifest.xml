<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.xihuxiaolong.generalcomponent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".toolbarshow.ToolbarShowActivity" />
        <activity android:name=".doubanmovielist.DoubanMovieListActivity" />
        <activity android:name=".shortnotelist.ShortNoteListActivity" />
        <activity android:name=".shortnoteedit.ShortNoteEditActivity" />
        <activity android:name=".citylist.CityListActivity" />
    </application>

</manifest>